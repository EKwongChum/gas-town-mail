# Structured JSON console logging (Spring Boot 3.4 built-in Logstash format).
# Activate with SPRING_PROFILES_ACTIVE=json; MDC fields such as traceId are included.
logging:
  structured:
    format:
      console: logstash
